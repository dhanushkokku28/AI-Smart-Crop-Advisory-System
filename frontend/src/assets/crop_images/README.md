# Dummy Crop Images

This folder is reserved for local crop image assets.
The current build uses network placeholders from Unsplash for quick prototyping.
You can replace them with local files and update the `image` fields in `src/services/api.js`.
